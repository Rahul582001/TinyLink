# TinyLink

TinyLink is a minimal URL shortener (assignment scaffold).

Run server:

```powershell
cd server
npm install
npm run dev
```

Run client:

```powershell
cd client
npm install
npm run dev
```

Environment variables: copy `.env.example` to `.env` in `server` and/or root and set `MONGODB_URI`.
